import '@testing-library/jest-dom';

/**
 * Global test setup for habit-tracker.
 *
 * Imports jest-dom matchers so all tests have access to DOM assertions
 * like toBeInTheDocument() and toHaveTextContent() without per-file imports.
 */
